from typing import List, Any
from langchain_core.messages import SystemMessage, HumanMessage
from core.llm_factory import get_llm  # ✅ Factory import

class ChunkFormatter:
    """RAG 검색 결과를 가독성 좋게 포맷팅"""
    
    def format_chunks(self, docs: List[Any], user_query: str) -> str:
        """
        검색된 문서들을 가독성 좋게 포맷팅
        """
        if not docs:
            return "❌ 관련 문서를 찾지 못했습니다."
        
        # 1) 문서 정보 수집
        chunks_info = []
        for idx, doc in enumerate(docs, 1):
            metadata = getattr(doc, "metadata", {}) or {}
            content = getattr(doc, "page_content", "")
            
            chunks_info.append({
                "idx": idx,
                "filename": metadata.get("source", "알 수 없음"),
                "section": metadata.get("section", ""),
                "content": content
            })
        
        # 2) LLM으로 요약 및 포맷팅
        formatted = self._format_with_llm(chunks_info, user_query)
        
        return formatted
    
    def _format_with_llm(self, chunks_info: List[dict], user_query: str) -> str:
        """LLM을 사용하여 검색 결과를 요약하고 포맷팅"""
        
        system_prompt = """
당신은 RAG 검색 결과를 사용자에게 보기 좋게 정리하는 Formatter입니다.

## 출력 형식

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 검색 결과 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 총 {n}개의 관련 문서를 찾았습니다.

💡 핵심 내용 요약:
[사용자 질의와 관련된 핵심 내용을 2-3줄로 요약]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 상세 문서 내용
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[문서 1] 파일명
─────────────────────────────────────────────
관련 내용 (200자 이내로 간결하게 요약)
─────────────────────────────────────────────

[문서 2] 파일명
─────────────────────────────────────────────
관련 내용 (200자 이내로 간결하게 요약)
─────────────────────────────────────────────

...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 지침:
1. 사용자 질의와 관련된 핵심 내용만 추출하세요
2. 각 문서는 200자 이내로 간결하게 요약하세요
3. 중요한 안전 규정, 수치, 기준은 반드시 포함하세요
4. 문서 출처(파일명)는 반드시 표시하세요
5. 전체적으로 가독성이 좋아야 합니다
"""
        
        # 문서 정보를 텍스트로 변환
        docs_text = "\n\n".join([
            f"[문서 {chunk['idx']}]\n"
            f"파일: {chunk['filename']}\n"
            f"내용: {chunk['content'][:500]}..."
            for chunk in chunks_info
        ])
        
        user_message = f"""
사용자 질의: {user_query}

검색된 문서들:
{docs_text}

위 검색 결과를 사용자에게 보기 좋게 정리해주세요.
"""
        
        try:
            # ✅ 수정됨: get_llm Factory 사용
            # 요약/정리는 'fast' 모드(Qwen)로 충분합니다. 품질이 부족하면 'smart'로 변경하세요.
            llm = get_llm(mode="fast") 
            
            messages = [
                SystemMessage(content=system_prompt.format(n=len(chunks_info))),
                HumanMessage(content=user_message)
            ]
            
            # LangChain invoke 호출
            response = llm.invoke(messages)
            
            return response.content
            
        except Exception as e:
            print(f"⚠️ LLM 포맷팅 실패: {e}")
            # Fallback: 간단한 포맷팅
            return self._simple_format(chunks_info)
    
    def _simple_format(self, chunks_info: List[dict]) -> str:
        """LLM 실패 시 간단한 포맷팅"""
        output = "━" * 60 + "\n"
        output += "📄 검색 결과 (요약 실패 - 원문 표시)\n"
        output += "━" * 60 + "\n\n"
        output += f"📌 총 {len(chunks_info)}개의 관련 문서를 찾았습니다.\n\n"
        output += "━" * 60 + "\n"
        output += "📚 상세 문서 내용\n"
        output += "━" * 60 + "\n\n"
        
        for chunk in chunks_info[:5]:  # 상위 5개만
            output += f"[문서 {chunk['idx']}] {chunk['filename']}\n"
            output += "─" * 60 + "\n"
            content = chunk['content'][:200].replace("\n", " ")
            output += f"{content}...\n"
            output += "─" * 60 + "\n\n"
        
        return output